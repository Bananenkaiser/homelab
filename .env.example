# Kopieren nach  .env  und den Pfad auf den Mountpoint der 2-TB-Platte setzen.
# Hier liegen alle großen/persistenten Daten (zim-Dateien, DB-Volumes, Medien).
# .env ist gitignored – der Pfad bleibt Pi-lokal.
APPDATA=/mnt/data/appdata
