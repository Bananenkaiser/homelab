# Kopieren nach  .env  und ggf. anpassen. .env ist gitignored (Pi-lokal).
#
# APPDATA = wo alle großen/persistenten Daten liegen (zim-Dateien, DB-Volumes,
# Medien). Auf dem Pi ist das die 2-TB-btrfs-Platte.
APPDATA=/srv/dev-disk-by-uuid-672d33ef-9522-48ce-a5ea-711cb8119569/appdata
